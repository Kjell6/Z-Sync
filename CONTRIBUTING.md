# Contributing

Thanks for taking an interest in Z-Sync.

New work should start as a GitHub issue, so it can be discussed before anyone writes a PR.

**Feature requests** — open an issue (use the Feature request form). If it gets a go, PRs are welcome. The change needs to land on iOS and Android together.

**Bugs** — open an issue first as well, then a PR that fixes it.
