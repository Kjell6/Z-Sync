{
  "contract": 1,
  "id": "http-keys-present-default",
  "purpose": "Collection-key bootstrap (payload present, default bundle only): GET /storage/crypto/keys returns 200 with X-Last-Modified. The encrypted envelope is built in test code from the crypto fixtures' fixed keys and is not recorded; the structural expectation records that a payload exists and covers the default key bundle.",
  "input": {
    "request": {
      "method": "GET",
      "path": "/storage/crypto/keys",
      "headers": {}
    }
  },
  "expect": {
    "response": {
      "status": 200,
      "headers": {
        "X-Last-Modified": "1700000100.00"
      },
      "body": null,
      "hasPayload": true,
      "collections": ["default"]
    }
  }
}
